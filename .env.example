SESSION_SECRET=pon_una_clave_larga_y_unica

# Propietario del sistema (unico que puede modificar reportes)
REPORT_OWNER_EMAIL=admin@reportes.local

# Ruta del archivo SQLite (opcional). En Render usa /var/data/reportes.db
# SQLITE_DB_PATH=./data/reportes.db

# Ollama (opcional, estos son los valores por defecto)
OLLAMA_HOST=http://127.0.0.1:11434
OLLAMA_MODEL=llama3.2
